<!-- GENERATED by benchmarks/generate_head_to_head_doc.py — edits here will be overwritten. Change the generator and regenerate. -->

# AgentArmor — Head-to-head Comparison

**Results as of: AgentArmor v1.4.0, run 2026-04-18**

> **DRAFT — infrastructure preview.** This document is regenerated automatically from the most recent run. The current run exercised only the mock baseline (CI fixture); real LlamaGuard / OpenAI Moderation / Perspective numbers land after the first empirical-calibration cycle (see [`RUNBOOK.md`](RUNBOOK.md) procedures 0 and 5). Tables below show the schema a reader should expect; entries are `—` until populated.

This document compares AgentArmor against three established safety/toxicity classifiers — LlamaGuard 3 (local, Q4_K_M), OpenAI Moderation, and Perspective API — across six industry datasets with per-sample verdicts and bootstrap confidence intervals. We publish per-dataset numbers only: no overall-winner claim, and we annotate every cell where AgentArmor loses or ties within CI. Reproduction: set the API keys listed in RUNBOOK.md and run `python -m benchmarks.run_head_to_head --run-dir benchmarks/results/runs/<ts>`.

### Per-dataset summary

| Dataset | Best baseline F1 | Best baseline | AgentArmor F1 | Δ vs best |
| --- | --- | --- | --- | --- |
| `advbench` | — | — | — | — |
| `harmbench` | — | — | — | — |
| `jailbreakbench` | — | — | — | — |
| `realtoxicity` | — | — | — | — |
| `toxigen` | — | — | — | — |
| `xstest` | — | — | — | — |

### Operating-point legend

| Baseline | Operating point |
| --- | --- |
| `mock_scored` | scripted scores (CI fixture only) |

## Per-dataset detail

### advbench

| Baseline | F1 | Precision | Recall | FPR | MCC | Bal-Acc | Latency p50 | Latency p95 | N | Errors |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `mock_scored` | — | — | 0.0000 | 0.0000 | — | — | 0.0 ms | 0.0 ms | 40 | 0 |

_AdvBench is reported with MCC + balanced-accuracy only; F1 omitted due to the 520:15 imbalance baseline-rate dominating F1 (SPEC v4 D38)._

### harmbench

| Baseline | F1 | Precision | Recall | FPR | MCC | Bal-Acc | Latency p50 | Latency p95 | N | Errors |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `mock_scored` | — | — | 0.0000 | 0.0000 | — | — | 0.0 ms | 0.0 ms | 40 | 0 |

### jailbreakbench

| Baseline | F1 | Precision | Recall | FPR | MCC | Bal-Acc | Latency p50 | Latency p95 | N | Errors |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `mock_scored` | — | — | 0.0000 | 0.0000 | — | 0.5000 (CI 0.5000–0.5000) | 0.0 ms | 0.0 ms | 40 | 0 |

### realtoxicity

| Baseline | F1 | Precision | Recall | FPR | MCC | Bal-Acc | Latency p50 | Latency p95 | N | Errors |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `mock_scored` | — | — | 0.0000 | 0.0000 | — | 0.5000 (CI 0.5000–0.5000) | 0.0 ms | 0.0 ms | 40 | 0 |

### toxigen

| Baseline | F1 | Precision | Recall | FPR | MCC | Bal-Acc | Latency p50 | Latency p95 | N | Errors |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `mock_scored` | — | — | 0.0000 | — | — | — | 0.0 ms | 0.0 ms | 40 | 0 |

### xstest

| Baseline | F1 | Precision | Recall | FPR | MCC | Bal-Acc | Latency p50 | Latency p95 | N | Errors |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `mock_scored` | — | — | 0.0000 | 0.0000 | — | 0.5000 (CI 0.5000–0.5000) | 0.0 ms | 0.0 ms | 40 | 0 |

## Appendices

### PR curves (appendix)

Precision–recall curves are rendered only for score-emitting `(baseline, dataset)` pairs on the six main-comparison datasets, and only when at least 50 positive samples are present (below that, bootstrap PR is too noisy to publish honestly — SPEC v4 D12).

**Why a PR curve is missing (D48):**

- `shield` (regex; bool-by-design, no curve)
- `exfiltration_guard` (regex; bool-by-design, no curve)
- `unicode_shield` (regex; bool-by-design, no curve)
- `llamaguard` (when the logprobs gate fell back to text-parse — cell scores 0/1 only; see D36 and TYPE_WALKTHROUGH.md V1)
- Cells with fewer than 50 positives (noise floor)

_Curves are not yet rendered in this preview; the schema retains the per-sample `pred_score` needed to generate them from `head_to_head_verdicts.jsonl`._

### Cells marked `does_not_apply`

The applicability rubric (`benchmarks/taxonomy_applicability.py`) declares a (baseline, dataset) pair inapplicable when the baseline's taxonomy does not meaningfully overlap the dataset's labeling construct. Those cells are omitted from the comparison tables above.

| Baseline | Dataset | Rationale |
| --- | --- | --- |
| `perspective` | `advbench` | AdvBench adversarial jailbreaks are not framed as toxic content; out of Perspective's scope. |
| `perspective` | `harmbench` | Perspective measures toxicity, not harmful-behavior elicitation. Jailbreaks and illicit-behavior requests are typically not toxic in surface form. |
| `perspective` | `jailbreakbench` | Jailbreak prompts are not inherently toxic; Perspective would systematically under-flag. |

### Methodology notes

- **Bootstrap F1 CI:** 5000 iterations, per-cell seed derived from `sha256(baseline|dataset)` (SPEC v4 D44/D45). `random.Random`, not numpy — survives minor-version bumps across the pinned `numpy<2.0`.
- **Per-metric degenerate guards (D41):** F1, MCC, and balanced-accuracy each have distinct zero-denominator conditions; cells flagged "insufficient data for CI" (`degenerate_flag=true`) report the point estimate alone.
- **Reproducibility tolerances (D16/D39):** `same_day_spread_95p` and `one_week_drift_max` are published once the calibration runs complete — see RUNBOOK.md for the procedure.
- **Hashes, not bodies (D49):** per-sample JSONL carries `raw_response: null` in committed artifacts. Use `--keep-raw-responses` for local debug only.
- **Live document:** this URL always reflects the latest tag. External links citing a specific version should pin by `git tag` (see Historical versions below).

### Historical versions

Each published version is an immutable git tag. To cite a specific version in blog posts or external links, use `https://github.com/<org>/AgentArmor/blob/<tag>/BENCHMARKS_HEAD_TO_HEAD.md`.

_(History table populated by `scripts/publish_head_to_head.sh` on each release.)_
